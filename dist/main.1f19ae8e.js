// modules are defined as an array
// [ module function, map of requires ]
//
// map of requires is short require name -> numeric require
//
// anything defined in a previous bundle is accessed via the
// orig method which is the require for previous bundles
parcelRequire = (function (modules, cache, entry, globalName) {
  // Save the require from previous bundle to this closure if any
  var previousRequire = typeof parcelRequire === 'function' && parcelRequire;
  var nodeRequire = typeof require === 'function' && require;

  function newRequire(name, jumped) {
    if (!cache[name]) {
      if (!modules[name]) {
        // if we cannot find the module within our internal map or
        // cache jump to the current global require ie. the last bundle
        // that was added to the page.
        var currentRequire = typeof parcelRequire === 'function' && parcelRequire;
        if (!jumped && currentRequire) {
          return currentRequire(name, true);
        }

        // If there are other bundles on this page the require from the
        // previous one is saved to 'previousRequire'. Repeat this as
        // many times as there are bundles until the module is found or
        // we exhaust the require chain.
        if (previousRequire) {
          return previousRequire(name, true);
        }

        // Try the node require function if it exists.
        if (nodeRequire && typeof name === 'string') {
          return nodeRequire(name);
        }

        var err = new Error('Cannot find module \'' + name + '\'');
        err.code = 'MODULE_NOT_FOUND';
        throw err;
      }

      localRequire.resolve = resolve;
      localRequire.cache = {};

      var module = cache[name] = new newRequire.Module(name);

      modules[name][0].call(module.exports, localRequire, module, module.exports, this);
    }

    return cache[name].exports;

    function localRequire(x){
      return newRequire(localRequire.resolve(x));
    }

    function resolve(x){
      return modules[name][1][x] || x;
    }
  }

  function Module(moduleName) {
    this.id = moduleName;
    this.bundle = newRequire;
    this.exports = {};
  }

  newRequire.isParcelRequire = true;
  newRequire.Module = Module;
  newRequire.modules = modules;
  newRequire.cache = cache;
  newRequire.parent = previousRequire;
  newRequire.register = function (id, exports) {
    modules[id] = [function (require, module) {
      module.exports = exports;
    }, {}];
  };

  var error;
  for (var i = 0; i < entry.length; i++) {
    try {
      newRequire(entry[i]);
    } catch (e) {
      // Save first error but execute all entries
      if (!error) {
        error = e;
      }
    }
  }

  if (entry.length) {
    // Expose entry point to Node, AMD or browser globals
    // Based on https://github.com/ForbesLindesay/umd/blob/master/template.js
    var mainExports = newRequire(entry[entry.length - 1]);

    // CommonJS
    if (typeof exports === "object" && typeof module !== "undefined") {
      module.exports = mainExports;

    // RequireJS
    } else if (typeof define === "function" && define.amd) {
     define(function () {
       return mainExports;
     });

    // <script>
    } else if (globalName) {
      this[globalName] = mainExports;
    }
  }

  // Override the current require with this new one
  parcelRequire = newRequire;

  if (error) {
    // throw error from earlier, _after updating parcelRequire_
    throw error;
  }

  return newRequire;
})({"main.js":[function(require,module,exports) {
var elevators = function elevators() {
  var ElevatorModel;
  var modelView;

  ElevatorModel = function () {
    function ElevatorModelView() {
      // construct for ElevatorModelView object with 2 properties: number of floors and array of elevators
      this.floors = 8;
      this.elevators = [{
        floor: 1,
        moving: false,
        waiting: false
      }, {
        floor: 1,
        moving: false,
        waiting: false
      }]; // me = elevatorModelView object

      var me = this; // create the button divs and timer for all the floor

      var buttons = function () {
        var results = []; // for each floor, create 2 buttons (up/down)

        for (var floor = 8; floor >= 1; floor += -1) {
          results.push("<div id = 'button-floor-".concat(floor, "' class='button-floor'>\n") + "<button class='button up' data-floor='".concat(floor, "' >") + ' <div class=\'up\'></div>' + '</button>\n' + '<p class="timer">0</p>\n' + "<button class='button down' data-floor='".concat(floor, "'>") + '<div class=\'down\'></div>' + '</button>\n' + '</div>');
        }

        return results;
      }().join(''); // append the results array (contains all the button divs for each floor) to html


      $('#buttons').empty().append($(buttons)) // adds class name 'on' when button is activated
      .off('click').on('click', 'button', function () {
        // this value = <button>
        if ($(this).hasClass('on')) {
          return;
        }

        $(this).toggleClass('on'); // when button is clicked ->

        return $(me).trigger('pressed', [{
          floor: parseInt($(this)[0].dataset.floor),
          dir: $(this).children().hasClass('up') ? 'up' : 'down'
        }]);
      });
    }

    ElevatorModelView.prototype.clearButton = function (floor, dir) {
      // beep when elevator arrives
      var snd = new Audio('data:audio/wav;base64,');
      snd.play(); // remove 'on' class name when elevator arrives

      return $("#button-floor-".concat(floor, " > button > div.").concat(dir)).parent().removeClass('on');
    };

    ElevatorModelView.prototype.closestIdleCar = function closestCar(floor) {
      console.log("Finding closest car to ".concat(floor, " from "), this.elevators); // return an array of the non-moving elevators and their distance from the floor. example- [[el1, 3], [el2, 1]]

      var availableElevator = function () {
        var freeElevators = [];
        var elevators = this.elevators;
        var car;

        for (var i = 0; i < 2; i += 1) {
          car = elevators[i];

          if (!car.moving && !car.waiting) {
            freeElevators.push([i + 1, Math.abs(floor - car.floor)]);
          }
        }

        return freeElevators;
      }.call(this); // if there are no available elevators


      if (!availableElevator) {
        return [];
      }

      if (availableElevator.length <= 1) {
        return availableElevator[0];
      } // returns an array of a single closest elevator and it distance


      return availableElevator[0][1] <= availableElevator[1][1] ? availableElevator[0] : availableElevator[1]; //console.log(`Closest car to ${floor} is ${closest} from ${availableElevator}`);
    };

    ElevatorModelView.prototype.moveCar = function (carArr, floor) {
      var car = carArr[0];
      var deferred;
      var myCars;
      myCars = this.elevators;
      var duration = 500 * Math.abs(myCars[car - 1].floor - floor);
      deferred = $.Deferred();

      if (this.elevators[car - 1].moving) {
        return deferred.reject();
      }

      if (floor < 1 || floor > this.floors) {
        return deferred.reject();
      } // moving elevator animation


      this.elevators[car - 1].moving = true;
      $("#elevator".concat(car, " .car")).animate({
        bottom: "".concat((floor - 1) * 46, "px")
      }, {
        duration: duration * 1.5,
        easing: 'swing',
        complete: function complete() {
          myCars[car - 1].floor = floor;
          myCars[car - 1].moving = false; // make elevator wait for 2 seconds after reaching destination

          myCars[car - 1].waiting = true;
          console.log("elevator ".concat(car, " is waiting"));
          var elevatorWait = setInterval(function () {
            console.log("elevator ".concat(car, " FINISHED waiting"));
            myCars[car - 1].waiting = false;
            clearInterval(elevatorWait);
          }, 2000);
          return deferred.resolve();
        }
      }).delay(75);
      $("#elevator".concat(car, " .car > .floorContainer")).animate({
        top: "".concat(-368 + floor * 46, "px")
      }, {
        duration: duration * 1.5,
        easing: 'swing'
      }).delay(75); // Timer handler - counting down in intervals of 0.5 second

      var numFloors = this.floors;
      var waitingTime = duration / 1000;
      var waitingCounter = setInterval(function () {
        document.getElementsByClassName('timer').item(numFloors - floor).innerHTML = waitingTime;
        waitingTime -= 0.5;

        if (waitingTime < 0) {
          clearInterval(waitingCounter);
        }
      }, 500);
      return deferred;
    };

    return ElevatorModelView;
  }(); // new instance of an Elevator model. has this functions in proto: clearButton, closestIdleCar, moveCar


  modelView = new ElevatorModel();
  $(modelView).on('pressed', function (e, _arg) {
    var floor = _arg.floor,
        dir = _arg.dir;
    console.log("Pressed ".concat(floor, "-").concat(dir));
    var closestFloor = modelView.closestIdleCar(floor); // true if there is an available elevator

    if (closestFloor) {
      return modelView.moveCar(closestFloor, floor).then(function () {
        return modelView.clearButton(floor, dir);
      }); // no avaiable elevators
    } else {
      document.querySelectorAll('[data-floor="' + floor + '"]')[0].classList.remove('on');
      document.querySelectorAll('[data-floor="' + floor + '"]')[1].classList.remove('on');
      alert('No free elevators, please wait...');
    }
  }); // initial elevators positioning

  modelView.moveCar([1, 0], 4);
  modelView.moveCar([2, 0], 1);
};

elevators.call(this);
},{}],"../node_modules/parcel-bundler/src/builtins/hmr-runtime.js":[function(require,module,exports) {
var global = arguments[3];
var OVERLAY_ID = '__parcel__error__overlay__';
var OldModule = module.bundle.Module;

function Module(moduleName) {
  OldModule.call(this, moduleName);
  this.hot = {
    data: module.bundle.hotData,
    _acceptCallbacks: [],
    _disposeCallbacks: [],
    accept: function (fn) {
      this._acceptCallbacks.push(fn || function () {});
    },
    dispose: function (fn) {
      this._disposeCallbacks.push(fn);
    }
  };
  module.bundle.hotData = null;
}

module.bundle.Module = Module;
var checkedAssets, assetsToAccept;
var parent = module.bundle.parent;

if ((!parent || !parent.isParcelRequire) && typeof WebSocket !== 'undefined') {
  var hostname = "" || location.hostname;
  var protocol = location.protocol === 'https:' ? 'wss' : 'ws';
  var ws = new WebSocket(protocol + '://' + hostname + ':' + "52038" + '/');

  ws.onmessage = function (event) {
    checkedAssets = {};
    assetsToAccept = [];
    var data = JSON.parse(event.data);

    if (data.type === 'update') {
      var handled = false;
      data.assets.forEach(function (asset) {
        if (!asset.isNew) {
          var didAccept = hmrAcceptCheck(global.parcelRequire, asset.id);

          if (didAccept) {
            handled = true;
          }
        }
      }); // Enable HMR for CSS by default.

      handled = handled || data.assets.every(function (asset) {
        return asset.type === 'css' && asset.generated.js;
      });

      if (handled) {
        console.clear();
        data.assets.forEach(function (asset) {
          hmrApply(global.parcelRequire, asset);
        });
        assetsToAccept.forEach(function (v) {
          hmrAcceptRun(v[0], v[1]);
        });
      } else if (location.reload) {
        // `location` global exists in a web worker context but lacks `.reload()` function.
        location.reload();
      }
    }

    if (data.type === 'reload') {
      ws.close();

      ws.onclose = function () {
        location.reload();
      };
    }

    if (data.type === 'error-resolved') {
      console.log('[parcel] ✨ Error resolved');
      removeErrorOverlay();
    }

    if (data.type === 'error') {
      console.error('[parcel] 🚨  ' + data.error.message + '\n' + data.error.stack);
      removeErrorOverlay();
      var overlay = createErrorOverlay(data);
      document.body.appendChild(overlay);
    }
  };
}

function removeErrorOverlay() {
  var overlay = document.getElementById(OVERLAY_ID);

  if (overlay) {
    overlay.remove();
  }
}

function createErrorOverlay(data) {
  var overlay = document.createElement('div');
  overlay.id = OVERLAY_ID; // html encode message and stack trace

  var message = document.createElement('div');
  var stackTrace = document.createElement('pre');
  message.innerText = data.error.message;
  stackTrace.innerText = data.error.stack;
  overlay.innerHTML = '<div style="background: black; font-size: 16px; color: white; position: fixed; height: 100%; width: 100%; top: 0px; left: 0px; padding: 30px; opacity: 0.85; font-family: Menlo, Consolas, monospace; z-index: 9999;">' + '<span style="background: red; padding: 2px 4px; border-radius: 2px;">ERROR</span>' + '<span style="top: 2px; margin-left: 5px; position: relative;">🚨</span>' + '<div style="font-size: 18px; font-weight: bold; margin-top: 20px;">' + message.innerHTML + '</div>' + '<pre>' + stackTrace.innerHTML + '</pre>' + '</div>';
  return overlay;
}

function getParents(bundle, id) {
  var modules = bundle.modules;

  if (!modules) {
    return [];
  }

  var parents = [];
  var k, d, dep;

  for (k in modules) {
    for (d in modules[k][1]) {
      dep = modules[k][1][d];

      if (dep === id || Array.isArray(dep) && dep[dep.length - 1] === id) {
        parents.push(k);
      }
    }
  }

  if (bundle.parent) {
    parents = parents.concat(getParents(bundle.parent, id));
  }

  return parents;
}

function hmrApply(bundle, asset) {
  var modules = bundle.modules;

  if (!modules) {
    return;
  }

  if (modules[asset.id] || !bundle.parent) {
    var fn = new Function('require', 'module', 'exports', asset.generated.js);
    asset.isNew = !modules[asset.id];
    modules[asset.id] = [fn, asset.deps];
  } else if (bundle.parent) {
    hmrApply(bundle.parent, asset);
  }
}

function hmrAcceptCheck(bundle, id) {
  var modules = bundle.modules;

  if (!modules) {
    return;
  }

  if (!modules[id] && bundle.parent) {
    return hmrAcceptCheck(bundle.parent, id);
  }

  if (checkedAssets[id]) {
    return;
  }

  checkedAssets[id] = true;
  var cached = bundle.cache[id];
  assetsToAccept.push([bundle, id]);

  if (cached && cached.hot && cached.hot._acceptCallbacks.length) {
    return true;
  }

  return getParents(global.parcelRequire, id).some(function (id) {
    return hmrAcceptCheck(global.parcelRequire, id);
  });
}

function hmrAcceptRun(bundle, id) {
  var cached = bundle.cache[id];
  bundle.hotData = {};

  if (cached) {
    cached.hot.data = bundle.hotData;
  }

  if (cached && cached.hot && cached.hot._disposeCallbacks.length) {
    cached.hot._disposeCallbacks.forEach(function (cb) {
      cb(bundle.hotData);
    });
  }

  delete bundle.cache[id];
  bundle(id);
  cached = bundle.cache[id];

  if (cached && cached.hot && cached.hot._acceptCallbacks.length) {
    cached.hot._acceptCallbacks.forEach(function (cb) {
      cb();
    });

    return true;
  }
}
},{}]},{},["../node_modules/parcel-bundler/src/builtins/hmr-runtime.js","main.js"], null)
//# sourceMappingURL=/main.1f19ae8e.js.map